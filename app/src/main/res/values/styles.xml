<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/plate</item>
        <item name="colorPrimaryDark">@color/darkChocolate</item>
        <item name="colorAccent">#ff9191</item>
    </style>
    <style name="PlayerTimerProgressBar" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@color/strawberry</item>
    </style>

</resources>
